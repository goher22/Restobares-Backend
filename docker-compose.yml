version: '1'
services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=goher
      - MONGO_INITDB_ROOT_PASSWORD=Cigoher22
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:

